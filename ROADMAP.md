# 路线图与候选清单

这份文档记录"想到但还没做"的事情，是想法的落脚点，不是承诺。
已完成的内容记录在 [`CHANGELOG.md`](CHANGELOG.md)；范围护栏见 §3「明确不做」。

想到新点子就往 §1 里加一条；开始做某一项时把它移入当前开发分支，做完后从这里删除并写进 CHANGELOG。

## 1. 候选功能（Backlog，不承诺时间）

### 日常与统计

- [ ] 周报 / 月报视图：本周完成率、各模块投入时间汇总（游玩时长、咨询时长、训练容量已有数据基础）
- [ ] 首页摘要卡片自定义排序（目前只能显隐）
- [ ] 更多键盘快捷键：快速新增（如 `Ctrl+N`）、页面切换（如 `Ctrl+1..9`）

### 模块增强

- [ ] 训练模板复制（"复制一份再改"比重建快）
- [ ] 餐食模板化：把常吃的一餐存为模板，一键套用到某天
- [ ] 咨询模块简单收入统计视图（按月汇总费用与结算状态，仍不做完整财务系统）
- [ ] 自媒体看板支持按平台筛选

### 技术与性能

- [ ] 全局搜索升级为 SQLite FTS5（数据量增大后 LIKE 会变慢；迁移需新增虚拟表与触发器）
- [ ] 首页"需要关注"改为服务端聚合接口（目前是客户端多查询拼装，已做窗口限界，数据量再大时下一步是服务端聚合）
- [ ] 服务端错误消息全部走错误码（目前 10 个常见码已覆盖，长尾错误仍是中文原文透传）

### 桌面版体验

- [ ] 应用图标（目前用 Electron 默认图标）
- [ ] macOS 公证 / Windows 代码签名（消除"未知开发者"提示；需要开发者账号与证书，有年费）
- [ ] 自动更新（electron-updater + GitHub Releases）
- [ ] 系统托盘常驻与开机自启（可选项，默认关闭）

## 2. 维护性事项（例行）

- [ ] 依赖升级：每季度过一遍 `npm outdated`（重点：Electron 大版本、better-sqlite3、Vite/React）
- [ ] Playwright 与 CI 镜像版本跟进
- [ ] 数据库体积观察：长期使用后检查 `VACUUM` 是否需要定期化

## 3. 明确不做（范围护栏）

继承自 [`PRD.md`](PRD.md) 第 8 节，本应用的价值在"本地、简单、可控"，以下方向除非重新立项否则不做：

- 用户注册、登录、多用户与协作
- 云同步、云数据库、多设备同步、公网部署、局域网共享
- 手机 App 或专门的移动端适配
- 自媒体平台数据自动抓取；GitHub、日历、邮箱等第三方集成
- AI 自动规划、自动写文案、智能营养识别
- 完整的 CRM / 财务 / 开票 / 会计系统
- 可穿戴设备与健康平台同步；在线食物营养数据库、扫码识别
- 消息推送、审批、共享；复杂自定义仪表盘与报表
