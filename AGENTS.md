# my_days（个人工作台）协作规则

> 本项目使用 Project Cairn 组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只应包含一行 `@AGENTS.md`，使 Claude Code 读取同一套规则；Codex 直接读取本文件。

## 项目一句话定位

本地单机版个人工作生活管理 App：九大模块（计划/自媒体/开发/咨询/健身/饮食/游戏）+ SQLite 单文件持久化与备份恢复 + 中英双语 + 四种界面外观 + Electron 桌面版。

> 本文件由 `cairn init` 生成，已填入本项目自身的定位与 provider 配置；其他项目复用前应先运行自己的 init。

## Init 配置

- 毕业 provider：暂无（暂缓对接——首次毕业时再连接知识库）
- 知识库索引：尚未配置
- 毕业目标：尚未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 若存在 `cairn/ROADMAP.md`，读取其中的路线图、当前焦点与开放问题（ROADMAP 可选，最小初始化的项目可能没有；本项目的产品路线图在根目录 `ROADMAP.md`）。
3. 读 `cairn/LOG.md` 最新几条记录（最新条目在最上方），了解近期进展与关键决策。
4. 按任务需要读取相关的 `cairn/` 知识专题文档。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 极少变更，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 桩 | 写一次，不再改动 |
| `cairn/ROADMAP.md` | 路线图与进展 | 原地更新，保持精炼 |
| `cairn/LOG.md` | 时间线日志 | 新条目加在最上方（最新在前），每条 ≤ 20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑点写入正文小节并用 `contains` 标注；修订在 LOG 留指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只增不改 |
| `cairn/Cited.md` | 知识库引用清单 | 只存指针，绝不复制原文 |

> 其余一切只在具体信号出现时创建（有决策要记录、有坑被解决、有目标跨越单次会话）——不预建空壳。工程资产（代码或流程消费的契约/配置/规格）不归本体系管理，留在代码树中，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则层面的冲突以本文件为准。
- 业务/设计结论以 `cairn/` 知识专题文档的最新记录为准，而非更早的 LOG 条目。

## 知识库消费反射

- 在开展"可复用内核（其产出或依赖的任何结论）够格毕业"的工作之前，先查阅本项目自己的 `cairn/` 知识专题文档；目前尚未连接外部知识库（provider 暂缓对接——见上方 Init 配置），外部索引检查与 `cairn/Cited.md` 引用在连接后启用。

## 文档协作规则

- 动手改动前，判断用户想要"讨论/建议"还是"直接改文档"；当对方说"先看看 / 先评估"时，先给分析——不要直接重写正式文档。
- 修正过往判断时，追加更正说明；不要静默覆盖。
- 未经确认的判断不要写成定论。

## 知识沉淀规则

- 每一次实质性推进后，在 `cairn/LOG.md` 顶部追加一条记录（摘要 + 指针）；结论沉淀到 `cairn/` 知识专题文档。
- **完成答复门禁**：在做出任何完成声明之前——包括但不限于工作已完成或已实现、已定稿、已更新、已同步、已验证或测试通过；问题已修复或已解决；交付物可用；工作已结束的表述及其语义等价说法——先执行 `references/maintenance.md` 中的 Cairn 检查点；只更新其触发矩阵要求的记录，核实后再答复。用户明确要求只读/不改动时，禁止任何 Cairn 写入。
- 跨项目可复用的经验，待知识库连接后经毕业机制沉淀（provider 暂缓对接——见上方 Init 配置）。

## 本项目硬性约定（改代码前必读）

架构与完整约定见 `docs/DEVELOPMENT.md`；速查：i18n 用 `t('中文')` 且词典补英文、枚举以中文规范值存库；表结构变更只新增 `server/migrations/NNN_xxx.sql` 并在 `tables.ts` 注册；业务删除走软删除；保持 127.0.0.1 监听与 Origin 校验；外观/语言/主题切换不得触碰业务数据；路径一律走 `MY_DAYS_*` 环境变量。文档同步：改接口更新 `docs/API.md`、改表更新 `docs/DATA_MODEL.md`、用户可见变化更新 README 双语版与 `CHANGELOG.md`、完成 `ROADMAP.md` 候选项后从清单移除。
