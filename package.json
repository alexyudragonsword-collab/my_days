{
  "name": "my-days",
  "version": "1.1.0",
  "private": true,
  "description": "个人工作生活管理 App（本地单机版）",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w server & npm run dev -w client & wait",
    "build": "npm run build -w server && npm run build -w client",
    "start": "node server/dist/index.js",
    "app:start": "node scripts/start-app.mjs",
    "test": "node --test tests/server.test.mjs",
    "test:e2e": "playwright test",
    "icons": "node scripts/make-icons.mjs",
    "verify": "npm run build && node scripts/check-i18n.mjs && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.2"
  }
}
