# 更新日志

## v1.1.0 — 2026-09-11

清空 `ROADMAP.md` 候选清单中除"需要付费证书"以外的全部条目。

### 新增

- **统计报表页**：周报 / 月报视图，完成率（分母排除已取消 / 已延期）、每日完成柱状图、游玩与咨询时长、训练容量、发布内容、日均热量与体重变化；服务端 `GET /api/report` 区间聚合
- **餐食模板**：把常吃的一餐存为模板，一键套用到任意日期的任意餐次（新增 `meal_templates` / `meal_template_foods` 表）
- **训练模板复制**：连动作一起复制一份再改
- **咨询收入统计**：按月汇总沟通记录中的费用与结算状态，可切换全部客户 / 当前客户
- **自媒体平台筛选**：看板可按平台过滤，平台清单由现有内容自动归纳
- **首页摘要卡片排序**：设置页可调整卡片顺序与显隐（纯界面偏好，不动业务数据）
- **更多键盘快捷键**：`N` 快速新增、`1`~`9` / `0` 切换页面、`?` 显示快捷键列表；输入框内不触发
- **桌面版应用图标**：`npm run icons` 生成（无第三方依赖的 PNG 生成脚本），含 macOS 托盘模板图
- **桌面版自动更新**：`electron-updater` 对接 GitHub Releases；Windows / Linux 自动下载后询问重启，macOS 未公证只引导手动下载
- **桌面版系统托盘**：托盘菜单含打开窗口、打开数据目录、检查更新、保存并退出；「关闭窗口时保留在托盘」与「开机自启」为默认关闭的可选项

### 改进

- **全局搜索改用 SQLite FTS5**（trigram 分词器 + 触发器同步）：中文关键词支持任意位置子串匹配，数据量增大后不再全表 LIKE；不足 3 个字符的关键词自动回退 LIKE
- **首页「需要关注」改为服务端聚合**（`GET /api/home/attention`）：原来是客户端五个查询拼装，现在一次请求返回，原因文案由客户端按语言渲染
- **服务端错误码全覆盖**：新增 `server/src/errors.ts` 统一错误码清单，未登记的底层异常归一为 `INTERNAL_ERROR` 并保留 `detail`；错误码与客户端词典的一致性由测试校验
- **应用内输入弹窗取代 `window.prompt`**：原有 9 处 `window.prompt` 在 Electron 桌面版中无效（Electron 不实现该 API），统一改为 `useUI().prompt()`，并支持日期选择、多行输入与校验
- 新增 `scripts/check-i18n.mjs` 并接入 `npm run verify`：漏补英文词典会直接让校验失败（补齐了 97 条新文案译文）
- 依赖按 semver 范围升级（Playwright、TanStack Query、react-router-dom 等）；大版本升级（React 19 / Vite 8 / Express 5 / Electron 44 / TypeScript 7）仍记在 `ROADMAP.md` 待评估

### 行为变更

- 恢复损坏备份的失败响应由 `500` 改为 `400`（错误码仍为 `BACKUP_INVALID`，当前数据同样不受影响）

## v1.0.0 — 2026-08-14

首个完整版本，按 `PRD.md` 实现全部第一版功能并扩展。

### 核心功能（PR #1）
- 九大模块：首页总览、今日计划、自媒体、开发工作、咨询工作、健身计划、饮食计划、游戏娱乐、数据与设置
- 单文件 SQLite 持久化（仅监听 127.0.0.1）；手动/每日自动备份、安全恢复、回收站、全局搜索（Ctrl/Cmd+K）、快速新增、保存状态提示
- 今日计划与各模块的来源关联：只存指针、标题实时解析、完成时可选同步

### 界面外观（PR #2）
- 新增「流光玻璃」「Notion 笔记」「Neo-Brutalism」三种独立外观，与浅色/深色主题正交（4 外观 × 2 主题）；切换只改样式不动数据

### 工程化（PR #2）
- API Origin 校验；SQL 迁移机制（`schema_migrations`）；双击启动器（健康检查复用 / 启动锁 / 旧服务安全退出 / 自动开浏览器）；平台标准数据目录（旧 `~/.my_days` 自动沿用）；「保存并退出」「打开数据目录」
- 导出升级为 ZIP（manifest + 全量 JSON + 每表 CSV）
- 测试体系：服务端集成测试 + Playwright e2e + 三系统 CI；`docs/ACCEPTANCE.md` 将 AC-001~040 映射到自动化检查

### 中英双语（PR #3）
- 界面语言可切换中文 / English；业务枚举以中文规范值存库、仅显示层翻译，切换语言不动任何业务数据

### 功能补全（PR #4）
- 备忘「已归档 / 已转换」视图；服务端错误码双语化；游玩计时跨天保护与「放弃本次计时」；首页查询限界（90 天 / 7 天窗口 + `since` 参数）；健身模板编辑不留孤儿行；从导出 ZIP 导入（完整替换 + 安全备份）；周视图逐日快速添加；备忘双提交防护
- MIT LICENSE、英文 README、AC-034 与导入导出闭环自动化测试

### 免 Node 桌面版（PR #4 / #5）
- Electron 壳 + esbuild 单文件服务端；与源码版共用数据目录
- CI 构建三系统安装包：macOS `.dmg`（arm64+x64）、Windows `.exe`、Linux `.AppImage`（未签名）；`v*` 标签、手动触发或 main 上打包相关文件变更时构建
